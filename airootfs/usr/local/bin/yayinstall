#!/bin/sh

# Install dependencies
sudo pacman -Sy --noconfirm base-devel git

# Clone yay repository
cd /tmp
git clone https://aur.archlinux.org/yay.git
cd yay

# Build and install yay
makepkg -si --noconfirm

# Clean up
cd ..
rm -rf yay

echo "yay successfull install."